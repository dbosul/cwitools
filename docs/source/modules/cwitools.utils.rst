.. _Utilities:

Utilities Module (cwitools.utils)
=======================================

The utilities module contains functions primarily intended for internal use (e.g. handling input, output, loading package data). These functions may still be useful for Python users constructing their own custom analysis pipelines out of CWITools functions.

.. automodule:: cwitools.utils
